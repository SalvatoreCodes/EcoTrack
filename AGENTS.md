# Expo SDK 54 — pinned

This project targets **Expo SDK 54** (`expo` pinned `54.0.8`). The test phone's Expo Go supports SDK 54 — do **not** bump to 55/56.

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.
